autor (id,ime,prezime) // id NOT NULL
drzava(id,naziv) //id NOT NULL, naziv NOT NULL
adresa (id, grad, drzava_id, broj_ulice,naziv_ulice,postanski_broj) //id NOT NULL,drzava_id NOT NULL
izdavac (id,ime,godina_osnivanja,adresa_id) // id NOT NULL, ime NOT NULL, adresa_id NOT NULL
distributer (id,ime,broj_telefona,adresa_id) // id NOT NULL, adresa_id NOT NULL, broj_telefona NOT NULL
kategorija (id,naziv,roditelj_id) // id NOT NULL, roditelj_id NULL
izdavac_distributer(distributer_id,izdavac_id) distributer_id,izdavac_id NOT NULL

knjiga (id,ISBN,naslov,broj_stranica,cena,distributer_id,izdavac_id,kategorija_id) // id NOT NULL, ISBN NOT NULL,
distributer_id NOT NULL, kategorija_id NOT NULL, izdavac_id NOT NULL

autor_knjiga (autor_id,knjiga_id) // autor_id NOT NULL, knjiga_id NOT NULL

vrsta_kupca (id, naziv) id NOT NULL, naziv ima vrednosti: Standardni ili VIP 
kupac (id,ime,prezime,broj_telefona,broj_transakcija,vrsta_kupca_id,adresa_id) // id NOT NULL, broj_telefona NOT NULL ,
vrsta_kupca_id NOT NULL, adresa_id NOT NULL
vip_kartica (id,datum_izdavanja,popust,jeAktivna,kupac_id) // id NOT NULL, kupac_id NOT NULL

vrsta_kupovine (id,naziv) // id NOT NULL naziv ima vrednosti: Prodavnica ili Online,
prodavac (id,ime,prezime) //id NOT NULL
racun (id,datum,ukupna_cena,vrsta_kupovine_id,prodavac_id,kupac_id,adresaZaDostavu_id) // id NOT NULL, 
prodavac_id NOT NULL, vrsta_kupovine_id NOT NULL, adresaZaDostavu_id NULL, kupac_id NOT NULL

stavka (id,kolicina,popust,jedinicna_cena, racun_id,knjiga_id) //id NOT NULL, racun_id NOT NULL, 
knjiga_id NOT NULL
